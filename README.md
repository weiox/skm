# skill-init

`skill-init` 是一组围绕 agent skills 本身的工作流技能包。

当前包含：

- `find-skills`
- `install-linked-agent-skills`
- `organize-agent-skills`

目标：

- 发现外部 skill
- 导入 skill 包到 `agent-hub`
- 整理本地散落的 skill
- 统一 `Codex` 与 `Claude Code` 的入口层
